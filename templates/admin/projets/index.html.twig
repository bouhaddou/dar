{% extends 'admin/base.html.twig' %}

{% block title %}المشاريع{% endblock %}

{% block body %}
 
   <div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1 class="m-0 text-dark">Gestion des Projets</h1>
          </div><!-- /.col -->
          <div class="col-sm-6">
            <ol class="breadcrumb float-sm-right">
              <li class="breadcrumb-item "><a href="{{ path('admin_dashboard') }}">Table de bord</a></li>
              <li class="breadcrumb-item active">Projets</li>
            </ol>
          </div><!-- /.col -->
        </div><!-- /.row -->
      </div><!-- /.container-fluid -->
  <div class="container">
    <h1 class="text-center mt-3 mb-2">Liste des Projets</h1>
    <a class="float-right btn btn-success mb-2" href="{{ path('projets_new') }}"><i class="fas fa-plus"></i> Ajouter un Projet</a>
    
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Image</th>
                <th>Description</th>
                <th>Prix</th>
                <th>Etat</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projet in projets %}
            <tr>
                <td>{{ projet.titre|slice(0,30) }}</td>
                <td class="text-center"><img style="max-height: 50px;border-radius: 20%;" src="{{ asset('/admin/upload/projet/'~ projet.image) }}" ></td>
                <td>{{ projet.description|slice(0,20)|raw }}</td>
                <td>{{ projet.prix }}</td>
                <td>{{ projet.etat }}</td>
                <td>{{ projet.setAt ? projet.setAt|date('d-m-Y') : '' }}</td>
                 <td>
                    <a class="text-primary btn" href="{{ path('projets_show', {'id': projet.id}) }}"><i class="fas fa-eye"></i></a>
                    <a class="text-success btn"  href="{{ path('projets_edit', {'id': projet.id}) }}"><i class="fas fa-edit"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        </div>
</div>
    
{% endblock %}
