{% extends 'admin/base.html.twig' %}

{% block title %}الأيتام{% endblock %}

{% block body %}
 
   <div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1 class="m-0 text-dark">إدارة الأيتام</h1>
          </div><!-- /.col -->
          <div class="col-sm-6">
            <ol class="breadcrumb float-sm-right">
              <li class="breadcrumb-item "><a href="{{ path('admin_dashboard') }}">لوحة القيادة</a></li>
              <li class="breadcrumb-item active">الأيتام</li>
            </ol>
          </div><!-- /.col -->
        </div><!-- /.row -->
      </div><!-- /.container-fluid -->
      <div class="container">
    <h1 class="text-center mt-3 mb-2"> تعديل معلومات اليتيم</h1>
            
            
             {{ form_start(form) }}
            {{ form_widget(form) }}
         <div class="row" dir="rtl">  
        
                    <div class="col">
                       <button class="btn btn-success mb-3"><i class="fas fa-plus"></i> {{ button_label|default('تعديل') }}</button>
                    </div>  {{ form_end(form) }}
                    <div class="col">
                        <a class="btn btn-primary " href="{{ path('orphelin_index') }}"><i class="fas fa-undo"></i> رجوع </a>
                    </div>
                    <div class="col">
                    {{ include('admin/orphelin/_delete_form.html.twig') }}
                    </div>
            </div>
        


    </div>
</div>
{% endblock %}
